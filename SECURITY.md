# Security

Do not commit deployment-specific configuration, hostnames, secrets, customer
names, room identifiers, user identifiers, image registry paths, or static
domain policy from a live environment.

Before publishing or mirroring this repository, run a content scan for
environment-specific terms and inspect the full diff manually.
